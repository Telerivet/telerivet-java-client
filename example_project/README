This is an example command line program using the Telerivet Java client library.

It prompts the user for their Telerivet API key, a phone number, and a message to send,
then sends the message via Telerivet.

Usage:

1. Build example project:
    cd example_project
    mvn package

2. Run example project:
    java -jar target/TelerivetAPITest-bundle.jar

Example output:
    Enter your API key: lkg4GdlSfkgjs2938x
    Phone number to send message to: +16505550123
    Message to send: hello world
    Message is queued, message id=SM11111111111111
